# PeaceEdu 4.1.0 实施计划

- 制定日期：2026-07-29
- 状态：已完成（自动化与静态验证）
- 对应审计：`docs/engineering/audit-2026-07-29.md`

## 完成定义

所有 P1 与可在当前静态架构内可靠完成的 P2 项均已实现；新增功能具备无 JavaScript或第三方失败时的合理降级；完整构建、链接、HTML、依赖与生成一致性检查通过；计划复核无遗漏；版本更新并提交。

## A. 发布内容与 SEO

- [x] 移除 `_most_popular` 的重复原始目录发布，仅保留 collection URL。
- [x] 停止发布与家庭教育无关且不完整的 `_documents` 遗留页面，但保留源文件。
- [x] 修复 sitemap 首页重复 URL。
- [x] 将 RSS 热门教程改为 PeaceEdu 当前 RSS 地址和通用、不过期的操作说明。
- [x] 增强生成物审计：重复 sitemap URL、canonical、页面标题/H1、HTTP 资源、占位域名和遗留目录。
- [x] 增强内部链接检查：校验同页及跨页锚点。

## B. 中文产品体验与业务内容

- [x] 主导航、首页按钮、分类/标签/归档、热门页、404 和条款统一为简体中文。
- [x] 首页增加“从这里开始”、主题入口、最新文章与搜索/全部文章 CTA。
- [x] 关于页补充使命、适用场景、服务边界、内容路径和联系入口，保留原方案图作为辅助材料。
- [x] 404 增加返回首页、浏览文章和打开搜索的恢复操作。
- [x] 删除页脚占位社交入口，保留真实可用链接。
- [x] 重写隐私与使用说明，使其与当前第三方服务和业务实际一致。

## C. 搜索与 AI 新功能

- [x] Algolia 正常时保持远程搜索。
- [x] CDN 或 Algolia API 失败时，自动读取 `/algolia-records.json` 提供本地搜索。
- [x] 为加载、空结果、失败与降级状态提供中文/英文可访问提示。
- [x] AI 助手改为用户点击后加载，增加加载中、失败、重试和邮件降级。
- [x] 将 AI bot ID、SDK URL 和提供方名称移入配置。
- [x] AI 页面加入第三方数据处理提示和非专业意见边界。

## D. 交互、可访问性与性能

- [x] 修正异步复制成功/失败状态并本地化文案。
- [x] 标题永久链接文案本地化。
- [x] 移动目录补充 `inert` 状态和焦点恢复。
- [x] 移动导航支持 Escape 与外部点击关闭。
- [x] 返回顶部按钮仅在有实际滚动需求时显示。
- [x] 搜索缩略图提供固定容器比例，减少布局偏移。
- [x] 固定外部 Font Awesome 版本，避免 `@latest` 带来的不可重复构建。

## E. 工程与发布

- [x] 将工程审计文档从站点发布物中排除。
- [x] 增加 `site:verify` / CI 生成一致性与生产依赖审计。
- [x] 更新 README 的架构、降级策略、第三方服务和验证说明。
- [x] 将版本更新到 4.1.0，同步 npm、锁文件、主题元数据和版权头。
- [x] 执行 `npm run site:check`、`npm audit --omit=dev`、`bundle check`、`git diff --check`。
- [x] 复核本计划的全部项目，补齐遗漏。
- [x] 提交全部已审计的前序升级与本次修复。

## 完成复核

第二轮复核没有发现计划中的未实现代码项，并额外修复了：

- 新标签筛选页与旧 `#标签` 链接之间的 260 处兼容回归；
- collection 页面无法关闭 taxonomy 的模板缺陷；
- Windows 构建不需要的时区数据依赖；
- 版本元数据与生成 JavaScript banner 的一致性校验。

最终自动化结果：30 个公开 HTML 页面、172 张图片、全部内部页面与锚点通过，npm 生产依赖为 0 个已知漏洞。真实浏览器会话在当前环境不可用，因此下方发布后人工烟雾测试保留为明确的部署检查，而不是标记为已执行。

## 发布后人工烟雾测试

本地浏览器控制不可用，以下项目必须在部署预览或生产站点人工检查：

- [ ] 360px、768px、1280px 宽度下导航、首页、文章、标签、搜索和 AI 页面无横向溢出。
- [ ] 仅用键盘完成菜单、搜索、标签、移动目录、复制和 AI 加载。
- [ ] Algolia 在线搜索与模拟离线/阻断第三方后的本地搜索均可用。
- [ ] Coze 加载成功；阻断 SDK 时显示可理解的失败与联系入口。
- [ ] Google Analytics、Disqus 和嵌入媒体仅在预期页面加载。
